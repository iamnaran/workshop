package com.android.workshop.activities;import androidx.appcompat.app.AppCompatActivity;import android.content.Intent;import android.os.Bundle;import android.view.View;import android.widget.Button;import android.widget.LinearLayout;import com.android.workshop.R;import com.android.workshop.constants.AppConstants;import com.android.workshop.helper.UpasargaActivity;public class LoginActivity extends UpasargaActivity implements View.OnClickListener {    private Button buttonLogin;    private LinearLayout signUpButtonParent;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_login);        initaliseView();        initaliseListeners();    }    @Override    public void initaliseView() {        buttonLogin = findViewById(R.id.button_login);        signUpButtonParent = findViewById(R.id.signup_button_parent);    }    @Override    public void initaliseListeners() {        buttonLogin.setOnClickListener(this::onClick);        signUpButtonParent.setOnClickListener(this::onClick);    }    @Override    public void onClick(View view) {        switch (view.getId()) {            case R.id.button_login://                Intent intent = new Intent(LoginActivity.this,PhotosActivity.class);//                intent.putExtra(AppConstants.PHOTOS_DATA,"");//                startActivity(intent);//                startActivity(new Intent(LoginActivity.this, PhotosActivity.class));                break;            case R.id.signup_button_parent:                break;        }    }}