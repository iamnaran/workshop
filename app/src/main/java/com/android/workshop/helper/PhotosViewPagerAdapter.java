package com.android.workshop.helper;import android.view.LayoutInflater;import android.view.View;import android.view.ViewGroup;import androidx.annotation.NonNull;import androidx.viewpager.widget.PagerAdapter;public class PhotosViewPagerAdapter extends PagerAdapter {//        @NonNull//        @Override//        public Object instantiateItem(@NonNull ViewGroup collection, int position) {//            LayoutInflater inflater = LayoutInflater.from(DetailActivity.this);//            ViewGroup layout = (ViewGroup) inflater.inflate(R.layout.item_details_photos, collection, false);//            ImageView headerImageView = layout.findViewById(R.id.image_view);////////            collection.addView(layout);//            return layout;//        }    @Override    public void destroyItem(@NonNull ViewGroup collection, int position, @NonNull Object view) {        collection.removeView((View) view);    }    @Override    public int getCount() {        return 5;    }    @Override    public boolean isViewFromObject(@NonNull View view, @NonNull Object object) {        return view == object;    }    @Override    public CharSequence getPageTitle(int position) {        return "";    }}